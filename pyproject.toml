[project]
name = "csnetwk-mp"
version = "0.1.0"
description = "UDP Socket Programming implementation of a Pokemon game"
requires-python = ">=3.13"
dependencies = [
    "pillow>=10.4.0",
    "windows-curses>=2.4.1",
]
